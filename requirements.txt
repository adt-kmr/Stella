# STELLA / Helios-Cortex runtime dependencies (backend + pipeline).
# Equivalent to `pip install -e .`; the ML stack is the `[ml]` extra
# (see pyproject.toml) because torch is heavy.
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
pandas>=2.2
pydantic-settings>=2.2
PyYAML>=6.0
httpx>=0.27